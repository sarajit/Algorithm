#!/bin/ksh## Trigger sendEnvironmentStatus_fullreval.ksh for monthly runs#WEEK_NUM=$((($(date --date='last Thursday' +%-d)-1)/7+1))echo "Week Count =  $WEEK_NUM "DAY=`date '+%a'`echo "Day = $DAY"echo "$WEEK_NUM != 2 "if  [[ `date +%m -d"last Thursday"` -eq `date +%m -d"this Thursday"` && $WEEK_NUM != 2 ]]; then  echo "It is weekly run this week, will stop."  #returnfiif [[ $DAY = "Tue" ]] && [[ `date +%m -d"last Thursday"` -ne `date +%m -d"this Thursday"` || $WEEK_NUM == 2 ]]; then  echo "It is Monthly run this week, will stop."  #returnfiecho " Scripts : "ho "It is weekly run this week, will stop."  #returnfiif [[ $DAY = "Tue" ]] && [[ `date +%m -d"last Thursday"` -ne `date +%m -d"this Thursday"` || $WEEK_NUM == 2 ]]; then ## Trigger sendEnvironmentStatus_fullreval.ksh for monthly runs#WEEK_NUM=$((($(date --date='last Thursday' +%-d)-1)/7+1))echo "Week Count =  $WEEK_NUM "DAY=`date '+%a'`echo "Day = $DAY"echo "$WEEK_NUM != 2 "if  [[ `date +%m -d"last Thursday"` -eq `date +%m -d"this Thursday"` && $WEEK_NUM != 2 ]]; then  echo "It is weekly run this week, will stop."  #returnfiif [[ $DAY = "Tue" ]] && [[ `date +%m -d"last Thursday"` -ne `date +%m -d"this Thursday"` || $WEEK_NUM == 2 ]]; then  echo "It is Monthly run this week, will stop."  #returnfiecho " Scripts : "
